column = [1; 2];
row = [10 20 30];
[expanded, mask] = expand_dynamic(column, row);
disp(expanded(2, 3) + sum(mask))
[scalar_sum, scalar_mask] = expand_dynamic(20, 22);
disp(scalar_sum + scalar_mask)
function [expanded, mask] = expand_dynamic(left, right)
expanded = left + right;
mask = left < right;
end
